import { type NextPage } from "next";
import Head from "next/head";
import Link from "next/link";

import { api } from "~/utils/api";

const shulteTable: NextPage = (sideLength: number) => {
    return (

    );
};

export default shulteTable;